// This file is for client builds.
// `ng build --configuration=staging` will use `environment.staging.ts`.
// The list of which env maps to which file can be found in `angular-cli
export const environment = {
    production: false,
    staging: true,
  };